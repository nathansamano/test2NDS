/* Nathan Samano
 * CMP 237
 * Problem 1
 */

// create global variable
var num = 0;

// infinite loop that breaks when num is 10
while(1) {
  print(num);
  if (num == 10)
    break;
  else
    num++;
}
